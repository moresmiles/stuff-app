<!DOCTYPE html>
<html lang="en">
<head>

  <!-- Basic Page Needs
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
  <meta charset="utf-8">
  <title>Stuff</title>
  <meta name="description" content="">
  <meta name="author" content="">

  <!-- Mobile Specific Metas
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- FONT
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
  <link href='//fonts.googleapis.com/css?family=Raleway:400,300,600' rel='stylesheet' type='text/css'>

  <!-- CSS
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
  <link rel="stylesheet" type="text/css" href="<%= url("/style.css")%>">

  <!-- Scripts
  –––––––––––––––––––––––––––––––––––––––––––––––––– -->
  <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>

</head>

<div class="container">

      <nav>
        <ul>
          <li><a href="/owners/<%= current_user.id %>">Home Page</a></li>
          <li><a href="/moves/<%= current_user.id %>">All Moves</a></li>

          <li class="dropdown">
            <a href="javascript:void(0)" class="dropbtn">Moves</a>
            <div class="dropdown-content">
              <% current_user.moves.each do |move| %>
              <a href="/moves/<%= move.id %>/boxes"> Move From <%=move.origin%> To <%=move.destination%></a>
                <%end%>
              </div>
            </li>
            <li class="dropdown">
              <a href="javascript:void(0)" class="dropbtn">Boxes</a>
              <div class="dropdown-content">
                <% current_user.boxes.each do |box| %>
                <a href="/moves/boxes/<%= box.id %>"><%=box.name%> (<%=box.move.origin%> To <%=box.move.destination%>)</a>
                <%end%>
              </div>
            </li>
            <li class="dropdown">
              <a href="javascript:void(0)" class="dropbtn">Items</a>
              <div class="dropdown-content">
                <% current_user.items.each do |item| %>
                <a href="/boxes/items/<%= item.id %>"><%=item.name%> (<%=item.box.move.origin%> To <%=item.box.move.destination%>)</a>
                <%end%>
              </div>
            </li>
          <li><a href="/logout">Logout</a></li>
        </ul>
      </nav>

  </div>

<body>
<% flash.keys.each do |type| %>
  <div data-alert class="flash <%= type %> alert-box radius">
    <%= flash[type] %>
    <a href="#" class="close">&times;</a>
  </div>
<% end %>
      <%= yield %>


</body>
</html>
